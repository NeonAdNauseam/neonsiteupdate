    <div class="wrapper">
          <div class="portfolio-container">
            <nav class="tab-menu">
                <button class="tab" data-index="0">Deepfake</button>
                <button class="tab" data-index="1">Lobster Lass</button>
                <button class="tab" data-index="2">Project 3</button>
                <button class="tab" data-index="3">Project 4</button>
                <button class="tab" data-index="4">Project 5</button>
                <button class="tab" data-index="5">Project 6</button>
            </nav>
            <div class="portfolio-display">
                <div class="portfolio-item active">
                    <iframe src="https://www.youtube.com/embed/swO7Cup2ag4" allowfullscreen></iframe>
                </div>
                <div class="portfolio-item">
                    <iframe src="https://www.youtube.com/embed/WJod64N1Muk" allowfullscreen></iframe>
                </div>
                <div class="portfolio-item">
                    <p>Content for Project 3</p>
                </div>
                <div class="portfolio-item">
                    <p>Content for Project 4</p>
                </div>
                <div class="portfolio-item">
                    <p>Content for Project 5</p>
                </div>
                <div class="portfolio-item">
                    <p>Content for Project 6</p>
                </div>
            </div>
        </div>
    </div>

<script is:inline>
const tabs = document.querySelectorAll(".tab");
const items = document.querySelectorAll(".portfolio-item");
let autoScroll = true;
let currentIndex = 0;
let interval;

function switchTab(index) {
    tabs.forEach(tab => tab.classList.remove("active"));
    items.forEach(item => item.classList.remove("active"));
    tabs[index].classList.add("active");
    items[index].classList.add("active");
    pauseAllVideos();
}

function pauseAllVideos() {
    document.querySelectorAll("iframe").forEach(iframe => {
        iframe.src = iframe.src; // Reset iframe to stop video
    });
}

function startAutoScroll() {
    interval = setInterval(() => {
        if (autoScroll) {
            currentIndex = (currentIndex + 1) % tabs.length;
            switchTab(currentIndex);
        }
    }, 5000);
}

tabs.forEach((tab, index) => {
    tab.addEventListener("click", () => {
        autoScroll = false;
        clearInterval(interval);
        switchTab(index);
    });
});

startAutoScroll();
</script>

    <style>
    
.wrapper {
    background: var(white);
    font-family: Arial, sans-serif;
    margin: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.portfolio-container {
    display: flex;
    background: var(--tabcontainer-color);
    border-radius: 15px;
    overflow: hidden;
    box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.2);
    max-width: 1300px;
    height: 45rem;
    width: 90%;
  }
  
  .tab-menu {
    display: flex;
    flex-direction: column;
    background: var(--tabcontainertwo-color);
    padding: 1rem;
    align-items: center;
    justify-content: start;
  }
  
  .tab {
    background: var(--inactive-tab);
    border: none;
    padding: 1rem;
    cursor: pointer;
    transition: background 0.3s;
    color: var(--text-color);
    font-size: 1rem;
    text-align: left;
    width: 100%;
  }
  
  .tab:hover,
  .tab.active {
    background: var(--active-tab);
  }
  
  .tab:first-child {
    border-top-left-radius: 10px;
  }
  
  .tab:last-child {
    border-bottom-left-radius: 10px;
  }
  
  .portfolio-display {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--card-color);
    padding: 1rem;
  }
  
  .portfolio-item {
    display: none;
    text-align: center;
  }
  
  .portfolio-item.active {
    display: block;
  }
  
  iframe {
    width: 60rem;
    height: 40rem;
    border: none;
    border-radius: 10px;
    box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.3);
  }
    </style>