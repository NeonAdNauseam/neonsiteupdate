<div class="face-inner">
  <div class="parallax-scene-4">
    <div class="scene-canvas" id="parallax-scene-4">
      <img
        src="/assets/face4/window.png"
        alt="Background"
        class="layer layer-bg"
      />
      <img
        src="/assets/face4/shelf.png"
        alt="Shop wall"
        class="layer layer-wall"
      />
      <img
        src="/assets/face4/avatar.png"
        alt="Neon behind counter"
        class="layer layer-avatar"
      />
      <img
        src="/assets/face4/speech-bubble.png"
        alt="Speech bubble"
        class="layer layer-bubble"
      />
      <img
        src="/assets/face4/counter.png"
        alt="Counter"
        class="layer layer-counter"
      />
    </div>
  </div>

  <!-- Printable button links overlay -->
<div class="portfolio-print-link">
  <div class="portfolio-print-link__buttons">
    <a href="/printablePortfolio" class="portfolio-print-link__button">
      Printable portfolio
    </a>
    <a
      href="https://www.youtube.com/@neonsmith2802"
      class="portfolio-print-link__button"
      target="_blank"
      rel="noreferrer"
    >
      YouTube
    </a>
    <a
      href="https://github.com/NeonAdNauseam"
      class="portfolio-print-link__button"
      target="_blank"
      rel="noreferrer"
    >
      GitHub
    </a>
    <a
      href="https://codepen.io/NeonAdNauseam"
      class="portfolio-print-link__button"
      target="_blank"
      rel="noreferrer"
    >
      CodePen
    </a>
    <a
      href="https://steamcommunity.com/id/The_Pale_Soldier/"
      class="portfolio-print-link__button"
      target="_blank"
      rel="noreferrer"
    >
      Steam
    </a>
  </div>
</div>




<style>
:root {
  --print-btn-bg: rgba(0, 0, 0, 0.8);
  --print-btn-bg-hover: rgba(0, 0, 0, 0.95);
  --print-btn-border: 1px solid rgba(255, 255, 255, 0.35);
  --print-btn-radius: 999px;
  --print-btn-shadow: 0 0.9rem 2.2rem rgba(0, 0, 0, 0.7);
  --print-btn-color: #f7f7ff;
  --print-btn-accent: #ffb3d9;
  --print-btn-padding-y: 0.75rem;
  --print-btn-padding-x: 1.9rem;
  --print-btn-font-size: 0.9rem;
  --print-btn-text: #f7f7ff;
}

/* existing styles */

.face-inner {
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  position: relative;
}

/* Face 4 parallax container sits behind everything, fills the face */
.parallax-scene-4 {
  position: absolute;
  inset: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  pointer-events: none;
  z-index: 0;
}

.parallax-scene {
  width: 100%;
  height: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  perspective: 800px;
}

.scene-canvas {
  width: 1280px;
  height: 720px;
  position: relative;
  transform: scale(calc(100vw / 1280));
  transform-origin: top left;
}

.layer {
  position: absolute;
  pointer-events: none;
  transition: transform 0.1s ease-out;
  will-change: transform;
}

/* Background fills the whole canvas, at the very back */
.layer-bg {
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  object-fit: cover;
}

/* Shelf in front of the background */
.layer-wall {
  width: 154.9%;
  top: -41.61%;
  left: -34.5%;
  z-index: 1;
}

.layer-avatar {
  width: 31.25%;
  top: 34.72%;
  left: 53.13%;
  z-index: 2;
}

.layer-bubble {
  width: 25%;
  top: 13.89%;
  left: 66.41%;
  z-index: 3;
}

.layer-counter {
  width: 84.22%;
  top: 71.28%;
  left: 6.28%;
  z-index: 4;
}

/* Printable portfolio + hub buttons overlay */
.portfolio-print-link {
  position: absolute;
  top: 50%;
  left: 7%;
  transform: translateY(-50%);
  z-index: 10;
  pointer-events: auto;
  display: flex;
  flex-direction: column;
  align-items: flex-start;

  .portfolio-print-link__buttons {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .portfolio-print-link__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: var(--print-btn-padding-y) var(--print-btn-padding-x);
    border-radius: var(--print-btn-radius);
    border: var(--print-btn-border);
    background: var(--print-btn-bg);
    color: var(--print-btn-text);
    font-size: var(--print-btn-font-size);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    text-decoration: none;
    box-shadow: var(--print-btn-shadow);
    cursor: pointer;
    pointer-events: auto;
    transition:
      background-color 0.12s ease,
      transform 0.12s ease,
      box-shadow 0.12s ease;

    &:hover {
      background: var(--print-btn-bg-hover);
      transform: translateY(-1px);
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.45);
    }

    &:active {
      transform: translateY(0);
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.35);
    }
  }
}
</style>

